// Shared layout vocabulary for the widget system. A widget occupies a fraction
// of the dashboard grid's row; the vertical list ignores span and stacks.
export type WidgetSpan = "quarter" | "half" | "full";
