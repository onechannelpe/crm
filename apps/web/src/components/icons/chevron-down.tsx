import { createIcon } from "./create-icon";

const iconNode = [["path", { d: "m6 9 6 6 6-6", key: "qrunsl" }]] as const;

const ChevronDown = createIcon("chevron-down", iconNode);

export default ChevronDown;
