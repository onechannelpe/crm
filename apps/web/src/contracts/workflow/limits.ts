// Product limit: up to three supporting files per revision.
export const MAX_RATE_REVISION_FILES = 3;

// Product limit: after three revision rounds, the lead is closed.
export const MAX_RATE_REVISION_ROUNDS = 3;

// Minimum GPV for a viable commercial prospect.
export const MIN_GPV = 1000;
