---
title: "Seeding por escenarios, UUID en organizaciones y flujo de venta por etapas"
date: "2026-04-30"
kind: release
cadence: nightly
visibility: internal
tags: ["workflow", "database", "sales"]
---

El bloque más grande del día fue datos y seeding. Se separó el esquema de seeds en escenarios y capas (`bootstrap` vs `demo`), con compiladores y persistencia por dominio (`core`, `organizations`, `workflow-leads`, `workflow-commercial`, `history-events`, `search-overlays`). Este cambio reemplaza el flujo monolítico previo y permite preparar datasets más controlados para desarrollo y pruebas.

Por el lado de la base de datos, las organizaciones fueron migradas a UUID. El ajuste impactó esquema, tipos y repositorios relacionados, además de pruebas de integración que dependen de locking, asignaciones y rutas de consulta sobre entidades organizacionales.

En workflow comercial, se partió la creación de venta en etapas y se añadió manejo de sedes (venues) con tab dedicado, formulario y repositorios específicos (`sale-venue-repo`, `create-sale-venue`). También se actualizó la sección comercial para usar un picker de banco y sostener el nuevo recorrido de entrada de datos.

Además de esas tres líneas principales, se actualizaron queries/presenters de workflow, fixtures y kits de prueba para mantener consistencia con los nuevos contratos de datos y comandos.

<Collapsible title="Mejoras">

- <UpdateLabel color="#26b5ce">Seeding</UpdateLabel> Nueva arquitectura por
  escenarios con separación `bootstrap` y `demo`.
- <UpdateLabel color="#4cb782">Database</UpdateLabel> Migración de
  organizaciones a UUID en esquema, tipos y acceso a datos.
- <UpdateLabel color="#f2c94c">Sales workflow</UpdateLabel> Creación de venta
  dividida por etapas con soporte de sedes (`venues`).
- <UpdateLabel color="#eb5757">UI comercial</UpdateLabel> Nuevo tab de sedes,
  formularios asociados y actualización de entrada comercial con bank picker.
- <UpdateLabel color="#95a2b3">Workflow infra</UpdateLabel> Nuevos comandos y
  repositorios para operaciones comerciales y detalle de lead.

</Collapsible>

<Collapsible title="Mantenimiento">

- <UpdateLabel color="#95a2b3">Tests</UpdateLabel> Ajustes amplios en
  integración, fixtures y test kits para el nuevo modelo de seeds y workflow.
- <UpdateLabel color="#95a2b3">Contratos</UpdateLabel> Actualización de tipos,
  presenters y consultas para alinear payloads e historial.
- <UpdateLabel color="#95a2b3">Estabilidad</UpdateLabel> Correcciones menores en
  import writers, políticas y repositorios relacionados.

</Collapsible>
