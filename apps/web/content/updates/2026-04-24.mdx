---
title: "Index de registros, DataGrid y acciones de panel"
date: "2026-04-24"
kind: release
cadence: nightly
visibility: internal
tags: ["record-index", "data-grid", "ui"]
---

El bloque principal de trabajo del día fue `record-index` y DataGrid. Se añadieron redimensionado de columnas, paginación y manejo dinámico de columnas, junto con controles nuevos en el menú de opciones para visibilidad de columnas y copia de enlace de vista. También se reestructuró el `view-bar` en componentes dedicados (`filter-menu`, `sort-menu`, `options-dropdown`, primitivas de menú) para separar responsabilidades y facilitar cambios futuros.

En el panel lateral se incorporó un menú de opciones en el footer con acciones según rol, y se ajustó la disponibilidad de reasignación para usar la verificación de permiso correcta (`canReassignLead`). En paralelo, se simplificó el scoping de supervisión eliminando equipos supervisados y se endurecieron pruebas/controles para que `lead:reassign` quede restringido a admin (a solicitud de Hector).

También entró una base nueva para imágenes responsivas con el paquete `packages/images` y su plugin de Vite, además de mejoras de overflow responsivo en tabs y ajustes de observación de resize/reactividad. En workflow y detalle de registro, se agruparon campos y se estandarizó el render de valores con componentes compartidos para reducir diferencias de presentación.

Como mantenimiento, se movió el seed monolítico a una estructura modular, se aplicaron ajustes de lint (`eslint-plugin-solid`) y pequeños fixes de estilos/etiquetas y referencias de assets.

<Collapsible title="Mejoras">

- <UpdateLabel color="#26b5ce">DataGrid</UpdateLabel> Se añadieron resize de
  columnas, paginación y gestión dinámica de columnas.
- <UpdateLabel color="#4cb782">Record index</UpdateLabel> El `view-bar` pasó a
  una arquitectura por componentes con menús de filtro, orden y opciones.
- <UpdateLabel color="#f2c94c">Vistas</UpdateLabel> El menú de opciones ahora
  permite alternar visibilidad de columnas y copiar enlace de vista.
- <UpdateLabel color="#eb5757">Panel lateral</UpdateLabel> El footer incorpora
  menú de acciones con controles por rol.
- <UpdateLabel color="#95a2b3">Permisos</UpdateLabel> Reasignación de leads usa
  `canReassignLead` y se reforzó restricción de `lead:reassign` para admin.
- <UpdateLabel color="#26b5ce">Imágenes</UpdateLabel> Se agregó el paquete
  `packages/images` con soporte de imágenes responsivas.
- <UpdateLabel color="#4cb782">Campos</UpdateLabel> Se agruparon campos de
  detalle y se estandarizó render de valores con componentes compartidos.

</Collapsible>

<Collapsible title="Mantenimiento">

- <UpdateLabel color="#95a2b3">Seeds</UpdateLabel> El seed fue reorganizado en
  archivos modulares para separar datos base y demo workflow.
- <UpdateLabel color="#95a2b3">UI</UpdateLabel> Se añadieron ajustes de overflow
  responsivo en tabs y simplificación de resize observation.
- <UpdateLabel color="#95a2b3">Tooling</UpdateLabel> Se configuró oxlint con
  `eslint-plugin-solid`.
- <UpdateLabel color="#95a2b3">Ajustes</UpdateLabel> Correcciones menores en
  estilos de panel, etiquetas y referencias de assets.

</Collapsible>
