---
title: "Negociación con archivos adjuntos y rediseño transaccional del workflow"
date: "2026-04-27"
kind: release
cadence: nightly
visibility: internal
tags: ["workflow", "negociacion", "observabilidad"]
---

La base técnica empezó con la migración de workflow a `commandApi/queryApi` y continuó con ejecución transaccional en Kysely. Ese cambio tocó varias áreas del servidor como runtime, repositorios, comandos, consultas y pruebas de integración para que las mutaciones compartan un camino consistente de ejecución y persistencia.

Sobre esa base se implementó el flujo de negociación: creación de rondas, soporte de archivos adjuntos con carga/descarga y almacenamiento asociado, y picker de bancos para operaciones de abono. A nivel de UI, el mayor impacto estuvo en `quoted-section.tsx`, `sale-section.tsx`, `quotation-section.tsx` y `commercial-input-section.tsx`, con ajustes de estructura y estilos para incorporar los nuevos estados y acciones.

Algo a destacar es que se añadió observabilidad con Sentry en cliente y servidor, incluyendo tracing y replay, junto con simplificación del manejo de errores. En paralelo entraron ajustes de consistencia en capacidades, invitaciones y consultas de workflow para mantener el comportamiento alineado con los nuevos límites transaccionales.

<Collapsible title="Mejoras">

- <UpdateLabel color="#26b5ce">Workflow core</UpdateLabel> Migración a
  `commandApi/queryApi` y uso de transacciones Kysely en operaciones de
  workflow.
- <UpdateLabel color="#4cb782">Negociación</UpdateLabel> Nuevo flujo con rondas
  y comandos específicos para solicitud de negociación.
- <UpdateLabel color="#f2c94c">Archivos</UpdateLabel> Adjuntos de negociación
  con upload/download y repositorio dedicado.
- <UpdateLabel color="#eb5757">Abonos</UpdateLabel> Se incorporó `bank picker` y
  reemplazo de input numérico en el tramo comercial.
- <UpdateLabel color="#95a2b3">UI workflow</UpdateLabel> Actualización amplia de
  secciones `quoted`, `sale`, `quotation` y `commercial-input`.
- <UpdateLabel color="#26b5ce">Observabilidad</UpdateLabel> Integración de
  Sentry (cliente/servidor) con tracing y replay.

</Collapsible>

<Collapsible title="Mantenimiento">

- <UpdateLabel color="#95a2b3">Infraestructura</UpdateLabel> Ajustes en
  runtimes, repositorios y contratos para soportar la ruta transaccional.
- <UpdateLabel color="#95a2b3">Pruebas</UpdateLabel> Se actualizaron tests de
  integración y kits de soporte para workflow y passkeys.
- <UpdateLabel color="#95a2b3">Políticas</UpdateLabel> Se alinearon reglas de
  acceso y validaciones en archivos/workflow con los cambios de arquitectura.
- <UpdateLabel color="#95a2b3">Consistencia</UpdateLabel> Correcciones menores
  en consultas de capacidad, invitaciones y configuración de engine.

</Collapsible>
