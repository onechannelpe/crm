name: web tests

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * *"
  push:
    branches:
      - "master"
    paths:
      - "apps/web/**"
      - "package.json"
      - ".oxlintrc.json"
      - ".oxfmtrc.json"
      - "bun.lock"
      - ".github/workflows/tests.yml"
  pull_request:
    branches:
      - "master"
    paths:
      - "apps/web/**"
      - "package.json"
      - ".oxlintrc.json"
      - ".oxfmtrc.json"
      - "bun.lock"
      - ".github/workflows/tests.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  default-tests:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false

      - uses: oven-sh/setup-bun@735343b667d3e6f658f44d0eca948eb6282f2b76 # v2.0.2
        with:
          bun-version: "1.3.9"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Generate test env
        run: |
          echo "SESSION_SECRET=$(openssl rand -hex 32)" >> .env
          echo "ENGINE_HMAC_SECRET=$(openssl rand -hex 32)" >> .env
          echo "ENGINE_URL=http://localhost:3001" >> .env
          echo "WEBAUTHN_RP_ID=localhost" >> .env
          echo "WEBAUTHN_ORIGIN=http://localhost:3000" >> .env
          echo "AUTH_RP_ID=localhost" >> .env
          echo "AUTH_ORIGIN=http://localhost:3000" >> .env

      - name: Typecheck + Oxc checks
        working-directory: apps/web
        run: bun run check

      - name: Run default tests
        working-directory: apps/web
        run: bun run test

  perf-tests:
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'

    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false

      - uses: oven-sh/setup-bun@735343b667d3e6f658f44d0eca948eb6282f2b76 # v2.0.2
        with:
          bun-version: "1.3.9"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Generate test env
        run: |
          echo "SESSION_SECRET=$(openssl rand -hex 32)" >> .env
          echo "ENGINE_HMAC_SECRET=$(openssl rand -hex 32)" >> .env
          echo "ENGINE_URL=http://localhost:3001" >> .env
          echo "WEBAUTHN_RP_ID=localhost" >> .env
          echo "WEBAUTHN_ORIGIN=http://localhost:3000" >> .env
          echo "AUTH_RP_ID=localhost" >> .env
          echo "AUTH_ORIGIN=http://localhost:3000" >> .env

      - name: Run perf tests
        working-directory: apps/web
        run: bun run test:perf
